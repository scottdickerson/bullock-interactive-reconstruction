---
import Analytics from '../components/Analytics';
import NavigationButtonsWrapper from '../components/NavigationButtonsWrapper';

interface Props {
  title?: string;
  pathname?: string;
}

const { title = 'Bullock Interactive Reconstruction'} = Astro.props;
const pathname = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Recounting Reconstruction - Bullock Interactive" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="pl-40 pb-24 pr-16 h-svh text-pretty">
    <Analytics client:load />
    <div
    class="absolute inset-0 bg-cover bg-center bg-no-repeat"
    style={{ backgroundImage: `url(/background.png)` }}
  ></div>
    <div class="[view-transition-name:content] relative h-full">
      <slot />
      <NavigationButtonsWrapper pathname={pathname} />
    </div>
   
  </body>
</html>

<style is:global>
  @import '../index.css';
</style>

